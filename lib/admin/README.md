# Flutter Admin Web

Run the same Flutter project for web and open `/admin` using your web host. The admin account must have the Firebase custom claim `admin: true`. The admin screens use Firebase Auth and Firestore directly and support dashboard counts, collection browsing, and order status updates.
