import 'package:flutter/material.dart';
import '../models/product.dart';
class ProductCard extends StatelessWidget { final Product product; final VoidCallback onTap; const ProductCard({super.key,required this.product,required this.onTap}); @override Widget build(BuildContext c)=>Card(child: InkWell(onTap:onTap,borderRadius:BorderRadius.circular(12),child:Padding(padding:const EdgeInsets.all(10),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Expanded(child:product.images.isEmpty?const Center(child:Icon(Icons.shopping_basket_outlined,size:55)):Image.network(product.images.first,fit:BoxFit.contain)),Text(product.name,maxLines:2,overflow:TextOverflow.ellipsis,style:const TextStyle(fontWeight:FontWeight.w600)),Text(product.unit,style:Theme.of(c).textTheme.bodySmall),const SizedBox(height:4),Row(children:[Text('₹${product.salePrice.toStringAsFixed(0)}',style:const TextStyle(fontWeight:FontWeight.bold)),const Spacer(),IconButton(onPressed:product.isAvailable?onTap:null,icon:const Icon(Icons.add_circle,color:Colors.green))])])))); }
