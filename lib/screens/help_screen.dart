import 'package:flutter/material.dart';
class HelpScreen extends StatelessWidget { const HelpScreen({super.key}); @override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Help & Support')),body:ListView(children:const [ExpansionTile(title:Text('How can I track my order?'),children:[Padding(padding:EdgeInsets.all(16),child:Text('Open My Orders and select an order to see its live status.'))]),ExpansionTile(title:Text('What is the delivery charge?'),children:[Padding(padding:EdgeInsets.all(16),child:Text('Delivery is free for orders above ₹499.'))]),ExpansionTile(title:Text('How do I contact support?'),children:[Padding(padding:EdgeInsets.all(16),child:Text('Email support@freshcart.example with your order number.'))])]);}
