# FreshCart Admin Panel

Replace `firebaseConfig` in `app.js` with Firebase Web App settings. Create an email/password admin user and assign the custom claim `{admin: true}` from a trusted server script. Serve this folder over a web server. The dashboard reads users, products, categories, orders, coupons and reviews, and supports order status updates.
